# Ignore derived / heavy artifacts
out/
# NCC maps and overlays are generated, exclude entire directory
out/ncc_maps/

# Research prototype bulk crops & masks
jana_code/crops/
jana_code/masks/

# Raw + duplicated imagery inside research prototypes (retain a minimal sample elsewhere if needed)
jana_code/slike/

# Generated dataset crops & templates
data/dataset_crops/
data/organism_templates/

# If raw source images should live outside git, uncomment below lines
# data/slike/
# Retain a small representative subset in e.g. data/slike_sample/

# Original ignore rule (kept): collected jpgs under pattern
collembolas*/*.jpg

# Excel / large spreadsheet outputs regenerated by scripts
*.xlsx

# Python cache
__pycache__/
